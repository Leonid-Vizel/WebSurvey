@model int
@{
    ViewData["Title"] = "Закрытие опроса";
}

<h2>Закрытие опроса #@Model</h2>
<div class="container justify-content-center">
    <form method="post">
    <a class="btn btn-outline-primary" asp-controller="Survey" asp-action="MySurveys"><i class="bi bi-arrow-return-left"></i> Назад</a>
        <button class="btn btn-outline-danger" type="submit"><i class="bi bi-door-closed"></i> Закрыть</button>
    </form>
</div>